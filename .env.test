# Test environment. Committed on purpose, so it must never contain real secrets.
# Point DATABASE_URL at a throwaway local database.
MEDUSA_ADMIN_ONBOARDING_TYPE=default
STORE_CORS=http://localhost:8000
ADMIN_CORS=http://localhost:5173,http://localhost:9000
AUTH_CORS=http://localhost:5173,http://localhost:9000,http://localhost:8000
JWT_SECRET=test-jwt-secret-not-for-production
COOKIE_SECRET=test-cookie-secret-not-for-production
DATABASE_URL=postgres://postgres:pari1501@localhost:5432/medusa-alter-commerce-test
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=pari1501